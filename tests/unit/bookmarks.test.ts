
// check out sinon-chrome for testing extensions
// it may be an old repository but it wouldn't hurt to test
// it out or to try and replicate its functinoality


// check if ts and jest play nice together
describe('thisIsAnExampleTest', () => {

  test('doesArithmeticWork', () => {
    const a = 1;
    expect(a).toBe(1);
  })

})
